{
  "name": "quantum-shield-frontend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "npx vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "axios": "^1.4.0",
    "lucide-react": "^0.435.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.0.0",
    "vite": "^5.0.0"
  },
  "scripts": {
  "prebuild": "npm install vite --force --no-audit --no-fund",
  "build": "npx vite build"
},
"scripts": {
  "prebuild": "npm install vite --force --no-audit --no-fund",
  "build": "npx vite build"
}


}
